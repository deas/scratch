apiVersion: v1
kind: Service
metadata:
  name: {{ include "h8des.fullname" . }}
  labels:
    {{- include "h8des.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "h8des.selectorLabels" . | nindent 4 }}
