apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "h8des.fullname" . }}-test-connection"
  labels:
    {{- include "h8des.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "h8des.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
